
#include "Hardware_Demos.h"

int main()
{
    // choose one feature to demo by uncommenting one of the demo functions
    // all demos enter an infinite loop and do not return.

    // demo_GPIO();
    // demo_PWM();
    // demo_SPI_0();
    demo_I2C();

    return 0;
}
